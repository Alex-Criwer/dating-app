<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/matches_item_root"
    android:layout_width="150dp"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="16dp"
    app:cardCornerRadius="10dp"
    app:strokeWidth="1dp"
    app:strokeColor="@color/primary_red">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
        <ImageView
            android:id="@+id/match_profile_photo"
            android:layout_width="match_parent"
            android:layout_height="200dp"
            android:src="@drawable/no_user_image"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"/>

        <ImageView
            android:id="@+id/is_match"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/sonder_match"
            android:layout_gravity="top|end"
            android:layout_margin="2dp"/>

        <TextView
            android:id="@+id/match_user_name_and_age"
            android:padding="2dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:text="Masha, 24"
            android:layout_gravity="bottom"
            android:layout_marginHorizontal="6dp"
            android:textColor="@color/primary_white"
            android:textAppearance="@style/MainFontStyle.Medium" />
    </FrameLayout>
</com.google.android.material.card.MaterialCardView>
